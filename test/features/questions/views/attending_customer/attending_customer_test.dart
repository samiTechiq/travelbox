

void main() {
  // late final AttendingCustomerViewModel viewModel;
  // late final Navigation navigation;
  // late final LoginViewmodel loginViewModel;
  //
  // registerStub() {}
  //
  // setUpAll(() {
  //   viewModel = MockAttendingCustomerViewModel();
  //   loginViewModel = MockLoginViewmodel();
  //   navigation = MockNavigation();
  //   when(viewModel.attendingCustomer).thenAnswer((realInvocation) => '');
  //   sl.registerLazySingleton<AttendingCustomerViewModel>(() => viewModel);
  //   sl.registerLazySingleton<Navigation>(() => navigation);
  //   sl.registerLazySingleton<LoginViewmodel>(() => loginViewModel);
  //   registerStub();
  // });

  // testWidgets('check that user can tap on AttendingCustomer radioButton',
  //     (WidgetTester tester) async {
  //   await tester.setScreenSize(height: 1200, width: 1000);
  //   await tester.setResources();
  //
  //   await tester.testAppForWidgetTesting(const AttendingCustomer());
  //   final button =
  //       find.byKey(Key(R.stringRes.attendingCustomerScreen.listAttendOne));
  //   await tester.ensureVisible(button);
  //   await tester.tap(button);
  // });
}
